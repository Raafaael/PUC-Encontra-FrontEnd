{
  "name": "puc-encontra-frontend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc && copy src\\styles.css dist\\styles.css",
    "watch": "tsc --watch",
    "serve": "python -m http.server 5173 --bind 127.0.0.1",
    "dev": "npm run build && npm run serve"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
